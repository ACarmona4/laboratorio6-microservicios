syntax = "proto3";

package books;

service BookService {
  rpc GetBookById (BookRequest) returns (BookResponse);
}

message BookRequest {
  string bookId = 1;
}

message BookResponse {
  bool exists = 1;
  string id = 2;
  string name = 3;
  int32 price = 4;
  int32 countInStock = 5;
}